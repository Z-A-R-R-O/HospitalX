"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
type Patient={id:string;full_name:string};
export default function NewBillPage(){const router=useRouter();const [patients,setPatients]=useState<Patient[]>([]);const [form,setForm]=useState({patientId:"",description:"",amount:""});const [status,setStatus]=useState("Loading patients…");useEffect(()=>{fetch("/api/patients").then(r=>r.json()).then(d=>{setPatients(d.patients??[]);setStatus("")}).catch(()=>setStatus("Could not load patients."))},[]);async function submit(e:FormEvent){e.preventDefault();setStatus("Saving bill…");const r=await fetch("/api/billing",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,amount:Number(form.amount)})});if(!r.ok){setStatus("Could not save bill.");return}setStatus("Bill saved.");setTimeout(()=>router.push("/"),700)}return <main className="form-shell"><button className="back" onClick={()=>router.push("/")}>← Back to Command Center</button><section className="form-card"><p>CITY CARE HOSPITAL</p><h1>Generate a bill</h1><span>Create a patient-linked billing record.</span><form onSubmit={submit}><label>Patient<select required value={form.patientId} onChange={e=>setForm({...form,patientId:e.target.value})}><option value="">{patients.length?"Select a patient":"No patients registered"}</option>{patients.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label>Description<input required value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Consultation, room, procedure…"/></label><label>Amount (INR)<input required min="0" step="0.01" type="number" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} placeholder="0.00"/></label><button className="primary" type="submit" disabled={!patients.length}>Save bill</button>{status&&<small className="form-status">{status}</small>}</form></section></main>}

"use client";
import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
export default function NewInventoryPage(){const router=useRouter();const [form,setForm]=useState({name:"",sku:"",category:"pharmacy",quantity:"",reorderLevel:"",unit:"unit"});const [status,setStatus]=useState("");async function submit(e:FormEvent){e.preventDefault();setStatus("Saving item…");const r=await fetch("/api/inventory",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,quantity:Number(form.quantity||0),reorderLevel:Number(form.reorderLevel||0)})});if(!r.ok){setStatus("Could not save item.");return}setStatus("Item saved.");setTimeout(()=>router.push("/dashboard"),700)}return <main className="form-shell"><button className="back" onClick={()=>router.push("/dashboard")}>← Back to Command Center</button><section className="form-card"><p>CITY CARE HOSPITAL</p><h1>Add inventory item</h1><span>Track pharmacy and hospital stock in Neon.</span><form onSubmit={submit}><label>Item name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Paracetamol 500mg"/></label><div className="form-grid"><label>SKU<input value={form.sku} onChange={e=>setForm({...form,sku:e.target.value})} placeholder="Optional"/></label><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option value="pharmacy">Pharmacy</option><option value="medical">Medical supply</option><option value="consumable">Consumable</option></select></label><label>Quantity<input type="number" min="0" required value={form.quantity} onChange={e=>setForm({...form,quantity:e.target.value})}/></label><label>Reorder level<input type="number" min="0" required value={form.reorderLevel} onChange={e=>setForm({...form,reorderLevel:e.target.value})}/></label></div><button className="primary" type="submit">Save item</button>{status&&<small className="form-status">{status}</small>}</form></section></main>}

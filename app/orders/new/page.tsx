"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
type Patient={id:string;full_name:string};
export default function NewOrderPage(){const router=useRouter();const [patients,setPatients]=useState<Patient[]>([]);const [form,setForm]=useState({patientId:"",orderType:"laboratory",testName:""});const [status,setStatus]=useState("Loading patients…");useEffect(()=>{const type=new URLSearchParams(window.location.search).get("type");if(type)setForm(f=>({...f,orderType:type}));fetch("/api/patients").then(r=>r.json()).then(d=>{setPatients(d.patients??[]);setStatus("")}).catch(()=>setStatus("Could not load patients."))},[]);async function submit(e:FormEvent){e.preventDefault();setStatus("Saving order…");const r=await fetch("/api/orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});if(!r.ok){setStatus("Could not save order.");return}setStatus("Order saved.");setTimeout(()=>router.push("/"),700)}return <main className="form-shell"><button className="back" onClick={()=>router.push("/")}>← Back to Command Center</button><section className="form-card"><p>CITY CARE HOSPITAL</p><h1>New clinical order</h1><span>Create a laboratory or radiology order for a patient.</span><form onSubmit={submit}><label>Patient<select required value={form.patientId} onChange={e=>setForm({...form,patientId:e.target.value})}><option value="">{patients.length?"Select a patient":"No patients registered"}</option>{patients.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label>Department<select value={form.orderType} onChange={e=>setForm({...form,orderType:e.target.value})}><option value="laboratory">Laboratory</option><option value="radiology">Radiology</option></select></label><label>Test or study<input required value={form.testName} onChange={e=>setForm({...form,testName:e.target.value})} placeholder="CBC, MRI, X-ray…"/></label><button className="primary" type="submit" disabled={!patients.length}>Create order</button>{status&&<small className="form-status">{status}</small>}</form></section></main>}
